#include <stdio.h>
#include <stdlib.h>
/**
 * add - Check
 * @a: integer a
 * @b: integer b
 * Description: function that adds two integers and returns the result
 * Return: Result to add a and b
 */
int add(int a, int b)
{
	return (a + b);
}
